package org.camunda.community.examples;

import org.camunda.bpm.model.xml.impl.instance.ModelTypeInstanceContext;

import io.camunda.zeebe.model.bpmn.builder.AbstractTaskBuilder;
import io.camunda.zeebe.model.bpmn.impl.instance.TaskImpl;
import io.camunda.zeebe.model.bpmn.validation.zeebe.BusinessRuleTaskValidation;

public class Test {
	
	public void sample()
	{
	/*	zeebeClient
		     .newEvaluateDecisionCommand()
		     .decisionKey("my-decision")
		     .variables(json)
		     .send();
		     
		     zeebeClient
     .newBroadcastSignalCommand()
   .signalName("signal")
     .variables(json)
     .send();
     
     zeebeClient
   *  .newResolveIncidentCommand(incidentKey)
   *  .send();
   *  
   *  zeebeClient
   *  .newUpdateTimeoutCommand(jobKey)
   *  .timeout(100)
   *  .send();
   *  
   *  
		     */
	}
	
}
